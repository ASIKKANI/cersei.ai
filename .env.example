# ==========================================
# Cersei.ai Environment Configuration (Optional)
# ==========================================

# Optional: Groq Cloud API Key for live LPU inference (gsk_...)
# If not set here, users can enter their key directly in the web UI when registering an agent.
VITE_GROQ_API_KEY=
